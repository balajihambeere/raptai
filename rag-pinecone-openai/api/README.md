### To run the application:
1. Build the container:
```bash
    docker-compose build
```
2. tart the services:
```bash
    docker-compose up
```
3. To stop the services:
```bash
    docker-compose down
```